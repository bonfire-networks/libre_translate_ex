import Config

# LibreTranslate configuration
# base_url is required, api_key is optional for self-hosted instances

config :libre_translate_ex,
  base_url: "https://libretranslate.com",
  api_key: nil  # Set your API key here if using libretranslate.com or an instance that requires it
